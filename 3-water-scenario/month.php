<?php
    session_start();

    // Use CSS Grid to display the employees so you can have two central columns.
    // Using CSS Grid means that when you add a new employee, he or she will always
    // be prominently located in the top left square, which is what you want.
    // That would be harder to do in flexbox.
?>