services:
  db:
    image: postgres:16-alpine
    container_name: jericho-dev-db
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev-pass
      POSTGRES_DB: jericho-dev

